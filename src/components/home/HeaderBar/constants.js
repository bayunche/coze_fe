// HeaderBar 组件相关常量

/**
 * Header 高度配置
 */
export const HEADER_CONFIG = {
  HEIGHT: '60px',
  PADDING: '0 32px'
}

/**
 * 面包屑导航配置
 */
export const BREADCRUMB_CONFIG = {
  SEPARATOR: '/',
  DEFAULT_ITEMS: [
    {
      label: '智能体控制台',
      path: '/'
    }
  ]
}

/**
 * Header 样式配置
 */
export const HEADER_STYLES = {
  BACKDROP_FILTER: 'var(--theme-backdrop-blur)',
  BOX_SHADOW: 'var(--theme-shadow-lg)',
  BORDER_BOTTOM: '1px solid var(--theme-border-secondary)',
  TRANSITION: 'all 0.3s ease'
}